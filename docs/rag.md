# RAG
Ingestion normalizes text and creates overlapping chunks. Retrieval currently uses a transparent keyword baseline. Embeddings, vector storage, hybrid search, PDF extraction, and reranking are explicit extension points.
