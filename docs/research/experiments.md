# Research Experiments

The research direction for PersonaAI focuses on adaptive routing, efficient retrieval, context management, local resource awareness, and privacy-first operation.

The initial experiments should compare:
- LLM-only baseline
- vector RAG
- hybrid RAG
- reranking + hybrid retrieval
- adaptive routing

All experiments should be kept under `experiments/` and should not modify production code paths directly.
